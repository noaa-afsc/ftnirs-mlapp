git add .
git commit -m "message" 
git tag vx.x.x
git push
git push origin vx.x.x


when updating the live app to the new tag, need to specify manually in the .env file to enforce the change. This will indicate to the dockerfile
which version to pull 